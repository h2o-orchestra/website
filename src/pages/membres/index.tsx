import * as React from "react";
import {PageProps} from "gatsby";
import {Layout} from "../../components/Layout";

const MemberPage: React.FC<PageProps> = () => {
    return (
        <Layout>
            <h1>Member page</h1>
        </Layout>
    )
}

export default MemberPage