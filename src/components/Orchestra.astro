---
interface Props {
    title: string;
    description: string;
}

const { title, description } = Astro.props;
---

<div class="bg-white p-6 md:p-8 rounded-2xl shadow-md h-full flex flex-col">
    <h2 class="text-2xl font-semibold text-h2o-dark mb-4">{title}</h2>
    <div class="prose prose-sm md:prose-base text-gray-600 max-w-none flex-grow">
        <p set:html={description}></p>
    </div>
    <a href={"orchestre"} class="flex justify-end text-pumpkin-500 hover:underline font-medium">
        En savoir plus
        <span aria-hidden="true">&nbsp;→</span>
    </a>
</div>